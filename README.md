# Teste Técnico

## Informacoes gerais

- O repositorio ja está com um banco de dados SQLite (`dbcontribuir.db`) pronto para uso.

- Foi criado teste unitario para a funcao `GetResumoPorContribuinte` (resumo de debitos).

- Foi utilizado log de `Warning` e `Error` em duas funcoes do servico de contribuintes.
